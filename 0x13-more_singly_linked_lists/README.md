We're continuing with our work on linked lists(singly-linked).
Further from adding nodes, counting nodes ,and freeing spaces allocated
to node elements, we'll be deleting nodes and inserting nodes and more.
