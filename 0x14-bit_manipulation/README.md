We started working on bit manipulation. Learning to use bitwise operators
&, |, ~, ^, >> << which are quite different from the
&&, || and ! logical operators.
It's quite amazing how much we can do just by manipulating bits.
