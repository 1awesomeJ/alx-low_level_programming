We started working with variadic functions- functions that take a variable
number and type of arguments.
so far, I've learnt about using ellipsis(...) in their declarations, including the <stdarg.h> header, the va_list variable type, the va_start, va_end and va_arg.
